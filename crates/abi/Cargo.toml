[package]
name = "ruuah-vt-abi"
version = "0.1.0"
edition.workspace = true

[lib]
name = "ruuah_vt"
crate-type = ["staticlib", "rlib"]

[dependencies]
ruuah-vt-abi-types = { path = "../abi-types" }
ruuah-vt-core = { path = "../core" }
ruuah-vt-snapshot = { path = "../snapshot" }

[dev-dependencies]
serde = { workspace = true }
toml = { workspace = true }
