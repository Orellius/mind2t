[package]
name = "mind2t-vt-difftest"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
# A binary, and it depends on the oracle crate above, so it is unpublishable for both reasons.
publish = false

[dependencies]
mind2t-vt-snapshot.workspace = true
mind2t-vt-ghostty.workspace = true
mind2t-vt-core.workspace = true
thiserror.workspace = true
serde.workspace = true
toml.workspace = true

[[bin]]
name = "difftest"
path = "src/main.rs"
